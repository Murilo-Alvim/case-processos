# URL pública do backend hospedado no Render.
# NÃO termine com /api — o cliente adiciona o sufixo.
# Em dev, deixe vazio para usar o proxy do Vite (vite.config.ts).
VITE_API_URL=https://processmap-api.onrender.com
